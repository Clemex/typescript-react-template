import * as React from 'react';
import { SFC } from 'react';

// Simplest stateless component with no properties.
// Notice no type is necessary, it is inferred. 
export const Dummy: SFC = () => (
  <div>
    I am but your humble servant. 
  </div>
);